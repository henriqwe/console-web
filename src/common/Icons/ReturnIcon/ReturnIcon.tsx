import { Icon } from '@iconify/react'

export function ReturnIcon({ className }: { className?: string }) {
  return (
    <Icon icon="icon-park-outline:return" className={`w-3 h-3 ${className}`} />
  )
}
